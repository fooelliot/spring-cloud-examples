spring:
  application:
    name: spring-cloud-refresh-bus
  cloud:
    config:
      uri: http://localhost:9000
      profile: dev
      label: master
      username: user
      password: password1232
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest