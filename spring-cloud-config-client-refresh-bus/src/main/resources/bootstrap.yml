spring:
  application:
    name: spring-cloud-refresh-bus
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/thethink/config_repo
          basedir: D:\tmp\config
  rabbitmq:
    host: localhost
    port: 5672
    username: cloud
    password: cloud
    virtual-host: /cloud