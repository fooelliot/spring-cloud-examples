spring:
#指的是springconfigserver的名称如果没有默认找到application.yml中的配置
  cloud:
    config:
      uri: http://localhost:9000
      profile: dev
      label: master
  rabbitmq:
    host: 192.168.121.10
    port: 5672
    username: guest
    password: guest