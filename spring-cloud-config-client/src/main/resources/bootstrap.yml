spring:
  application:
    name: spring-cloud-config-client
  cloud:
    config:
      discovery:
        enabled: true
        service-id: spring-cloud-config-server
      profile: pro
#      uri: http://localhost:9000
#      label: master
#      name:
#      username: user
#      password: password