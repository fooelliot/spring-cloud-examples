server:
  port: 8001
  
eureka:
  client:
    healthcheck:
      enabled: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
      
spring:
  application:
    name: springCloud-bus
  rabbitmq:
    host:
    port:
    username:
    password:

