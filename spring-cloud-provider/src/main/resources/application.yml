server:
  port: 8001
  
eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://user:password123@localhost:8761/eureka
  instance:
#    ip-address: true
#    instance-id: ${spring.application.name}:${spring.application.name}:${server.port}
    metadata-map:       #自定义元数据
      zone: ABC
      ruolin: BBC
#    lease-renewal-interval-in-seconds: 5
#    prefer-ip-address: true
#    appname: USER-SERVICE-HAHA
#    hostname: user
    #调整状态页面
    status-page-url-path: /health
    #调整将康检查的页面
#    health-check-url-path: /beans

logging:
  level:
    root: INFO
    org.hibernate: INFO
    org.hibernate.type.description.sql.BasicBinder: TRACE
    org.hibernate.type.description.sql.BasicExtractor: TRACE
    com.jja: DEBUG
    
spring:
  application:
#    name: springCloud-provider-v1
    name: springCloud-provider
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
  jackson: 
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: UTC
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cloud
    username: root
    password: root
management:
  security:
    enabled: false
